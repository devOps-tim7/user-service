export enum RelationType {
  Follow = 1,
  Mute = 2,
  Block = 3,
}

export enum Gender {
  Male = 1,
  Female = 2,
  Other = 3,
}

export enum Role {
  User = 1,
  Agent = 2,
  Admin = 3,
}
